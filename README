Shacknews Chatty Stats
======================

Crawls Shacknews API, consumes post information, populates database, exposes data via API, web interface consumes it.

Instructions
------------
* mvn install
* mvn exec:java -Pcrawler (for crawler)
* mvn exec:java -Papp (for app)